启动前需安装 Python 依赖：`pip install -r wakeword_runtime/requirements.txt`；然后通过 `python start.py` 启动，默认监听 8006 端口，访问 `http://127.0.0.1:8006/index.html` 打开测试页面，WebSocket 事件桥地址为 `ws://127.0.0.1:8006/wakeword-ws`。