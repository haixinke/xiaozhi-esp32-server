SenseVoiceSmall 通过 `demo.py` 使用 `funasr.AutoModel(model=model_dir, vad_model="fsmn-vad", hub="hf")` 加载本地模型目录；Silero VAD 可通过 `torch.hub.load('snakers4_silero-vad', 'silero_vad', onnx=False)` 或直接 import `silero_vad.load_silero_vad()` 加载，需确保 `src` 目录在 Python path 中。