<view class="page"><nav-bar title="我的激活码"></nav-bar><scroll-view class="content" scroll-y><view class="intro"><text class="title">把一颗蛋送给朋友</text><text class="desc">你有一个专属激活码，可邀请朋友各领养一只蛋宝宝。每位朋友只能使用一次，使用后扣减一次剩余次数。</text></view><view wx:if="{{inviteCode}}" class="code-card {{exhausted ? 'code-card--exhausted' : ''}}"><view class="code-head"><text class="code-label">我的激活码</text><text class="usage">已用 {{inviteCode.usedCount}} / {{inviteCode.quota}} · 剩余 {{inviteCode.remaining}}</text></view><text class="code">{{inviteCode.code}}</text><view class="code-foot"><text class="state">{{exhausted ? '已用完' : '可邀请'}}</text><button class="copy" disabled="{{exhausted}}" bindtap="onCopy">复制激活码</button></view></view><view wx:else class="empty">添加第一只蛋宝宝后，激活码会出现在这里。</view><view class="note">个人激活码不提供积分、排行榜或现金奖励。只有朋友成功绑定蛋宝宝后，才会扣减一次剩余次数。</view></scroll-view></view>
