微信支付真实模式需先在管理后台 sys_params 写入 6 项 wechat.pay.* 参数（private_key 与 api_v3_key 需经 AESUtils.encrypt(serverSecret, ...) 加密），并将 wechat.pay.mock 设为 false；本地 dev 环境需在 application.yml 的 spring.profiles.active: dev 段落添加 wechat.pay.mock: true 以保持 MockWechatPayClient 生效。