- 模型加载统一通过工厂函数封装（`load_silero_vad`、`AutoModel`），根据 onnx 参数动态选择 .jit 或 .onnx 权重路径。
- 推理前对输入音频进行采样率归一化到 16000Hz，并在 `read_audio` 中优先尝试 sox_effects 后回退到 torchaudio.load。
- VAD 结果以字典形式返回 start/end 时间戳，支持 return_seconds 切换样本数与秒单位。
- ONNX 推理通过 `OnnxWrapper` 类封装 onnxruntime.InferenceSession，强制设置 inter_op/intra_op 线程数为 1 避免多线程竞争。