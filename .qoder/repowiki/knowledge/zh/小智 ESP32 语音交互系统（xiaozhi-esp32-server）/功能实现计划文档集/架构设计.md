本模块为纯文档型 leaf 模块，存放以 Markdown 形式编写的跨端实现计划，按功能维度组织：
- `companion-module.md`：manager-api 中 companion 模块的完整开发步骤（Liquibase 迁移 → Entity/DAO → DTO/VO → Service → Controller → Shiro 权限 + ErrorCode），依赖 Spring Boot 3.4.3、MyBatis-Plus、Liquibase，参考 pet 模块结构。
- `subscription-popup-plan.md`：小程序 settings 页面订阅弹窗的前端实现计划，基于现有 destiny 页面的底部抽屉模式，定义 `/subscription/plans` 公共接口契约。
- `wechat-pay-v3-real-impl.md`：在已有支付骨架上引入 wechatpay-java SDK，新增 WechatPayV3Client 实现 WechatPayClient 接口，通过 sys_params 动态加载配置并 AES 加密敏感字段，配合 Liquibase 注入默认参数项。
- 其他文件如 `companion-setup-aggregation.md`、`companion-subscription-items-payment.md`、`mini-companion-codes.md`、`mini-companion-create-api.md`、`miniprogram-destiny-page.md`、`scenario2-dynamic-video.md`、`server-pipeline-stall-analysis.md`、`memory-anchor-page.md` 分别覆盖配套设置聚合、订阅商品支付流程、小程序代码片段、创建 API、命运页、动态视频场景、服务端流水线卡顿分析、记忆锚点页等子需求。
所有文档遵循统一的 Step-by-Step 格式：目标 → 上下文 → 依赖图 → 文件清单 → 验证方法 → 回滚策略，便于直接转化为 PR。