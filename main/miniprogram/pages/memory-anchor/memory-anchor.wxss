/* pages/memory-anchor/memory-anchor.wxss */
/* Ethereal Companion Design System - 记忆锚定 */

page {
  background-color: #fbf9f8;
  color: #1b1c1c;
}

.page {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #fbf9f8;
  background-image:
    radial-gradient(at 0% 0%, rgba(255, 183, 197, 0.2) 0px, transparent 50%),
    radial-gradient(at 100% 100%, rgba(211, 198, 218, 0.2) 0px, transparent 50%);
}

/* -------- 自定义导航栏 -------- */
.nav-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1rpx solid rgba(134, 78, 90, 0.08);
}

.nav-bar-inner {
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.nav-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #864e5a;
  letter-spacing: 2rpx;
}

/* -------- 滚动内容区 -------- */
.scroll-content {
  flex: 1;
  overflow: hidden;
  padding-top: calc(var(--status-bar-height, 44px) + 88rpx + 24rpx);
}

/* -------- 视频区 -------- */
.video-section {
  padding: 0 40rpx;
  margin-bottom: 40rpx;
}

.video-player {
  width: 100%;
  height: 820rpx;
  box-shadow: 0 8rpx 60rpx rgba(255, 183, 197, 0.35);
}

.video-wrap {
  position: relative;
  width: 100%;
  height: 820rpx;
  border-radius: 32rpx;
  overflow: hidden;
}

.video-replay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 32rpx;
}

.video-replay-btn {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.85);
  position: relative;
}

.video-replay-btn::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 54%;
  transform: translate(-50%, -50%);
  border-style: solid;
  border-width: 18rpx 0 18rpx 32rpx;
  border-color: transparent transparent transparent #864e5a;
}

/* -------- 通用 section -------- */
.section {
  margin-bottom: 48rpx;
  padding: 0 40rpx;
}

.section-header {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 12rpx;
}

.section-bar {
  width: 6rpx;
  height: 36rpx;
  background: #ffb7c5;
  border-radius: 4rpx;
  flex-shrink: 0;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1b1c1c;
  line-height: 1.3;
}

.section-subtitle {
  display: block;
  font-size: 24rpx;
  color: rgba(134, 78, 90, 0.6);
  margin-bottom: 28rpx;
  padding-left: 22rpx;
  line-height: 1.5;
}

/* -------- 选项网格 -------- */
.option-grid {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.pet-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

.option-item {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 28rpx 12rpx;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border: 2rpx solid rgba(255, 255, 255, 0.4);
  transition: all 0.25s ease;
  user-select: none;
}

.option-item-hover {
  transform: scale(0.95);
}

.option-item-active {
  background: rgba(255, 183, 197, 0.25);
  border-color: #864e5a;
  box-shadow: 0 0 20rpx rgba(255, 183, 197, 0.3);
}

.option-label {
  font-size: 26rpx;
  font-weight: 500;
  color: #1b1c1c;
  text-align: center;
  line-height: 1.3;
}

.option-item-active .option-label {
  color: #864e5a;
  font-weight: 600;
}

/* -------- 名字输入框 -------- */
.name-input-wrap {
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(20rpx);
  -webkit-backdrop-filter: blur(20rpx);
  border: 2rpx solid rgba(255, 255, 255, 0.4);
  border-radius: 24rpx;
  padding: 24rpx 32rpx;
}

.name-input {
  font-size: 28rpx;
  color: #1b1c1c;
  width: 100%;
}

/* -------- CTA 按钮 -------- */
.cta-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 16rpx 40rpx 24rpx;
  background: rgba(251, 249, 248, 0.85);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  z-index: 100;
}

.cta-btn {
  width: 100%;
  padding: 32rpx 0;
  border-radius: 100rpx;
  background: linear-gradient(135deg, #ffb7c5 0%, #ff9aa2 100%);
  text-align: center;
  box-shadow: 0 8rpx 40rpx rgba(255, 183, 197, 0.35);
  animation: softPulse 3s infinite ease-in-out;
  user-select: none;
}

.cta-btn-hover {
  transform: scale(0.97);
  opacity: 0.9;
}

.cta-btn-disabled {
  opacity: 0.5;
  animation: none;
  box-shadow: none;
}

.cta-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #360c19;
  letter-spacing: 2rpx;
}

@keyframes softPulse {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.01); opacity: 0.95; }
}

/* -------- 创造完成过渡 -------- */
.completion-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 999;
  background: #000;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: completion-overlay-enter 0.6s ease forwards;
}

.completion-glow {
  position: absolute;
  width: 600rpx;
  height: 600rpx;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(255, 183, 197, 0.06) 0%, transparent 70%);
  opacity: 0;
  animation: completion-glow-breathe 4s ease-in-out infinite alternate;
  animation-delay: 0.6s;
}

@keyframes completion-overlay-enter {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes completion-glow-breathe {
  0% { transform: scale(0.8); opacity: 0.3; }
  100% { transform: scale(1.3); opacity: 0.7; }
}

.completion-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  z-index: 1;
}

.completion-line {
  display: block;
  font-size: 44rpx;
  font-weight: 300;
  color: rgba(255, 243, 240, 0.92);
  letter-spacing: 8rpx;
  line-height: 2.4;
  opacity: 0;
}

.completion-line-1 {
  animation: completion-text-enter 0.8s ease-out forwards;
  animation-delay: 1.2s;
}

.completion-line-2 {
  animation: completion-text-enter 0.8s ease-out forwards;
  animation-delay: 2.6s;
}

.completion-separator {
  width: 0;
  height: 2rpx;
  background: rgba(255, 243, 240, 0.25);
  border-radius: 2rpx;
  margin: 32rpx 0;
  animation: completion-line-enter 0.6s ease-out forwards;
  animation-delay: 2.1s;
}

@keyframes completion-text-enter {
  0% {
    opacity: 0;
    transform: translateY(20rpx);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes completion-line-enter {
  0% {
    width: 0;
    opacity: 0;
  }
  100% {
    width: 60rpx;
    opacity: 1;
  }
}

/* -------- 安全区 -------- */
.safe-area-bottom {
  padding-bottom: env(safe-area-inset-bottom, 24rpx);
}
