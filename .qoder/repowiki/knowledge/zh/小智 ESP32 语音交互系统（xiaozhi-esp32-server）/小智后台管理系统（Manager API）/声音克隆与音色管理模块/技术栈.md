Spring Boot + MyBatis-Plus（`@TableName`/`@TableId`/`FieldFill` 自动填充）、Apache Shiro（`@RequiresPermissions` 权限注解）、Swagger v3（`@Operation`/`@Tag`/`@Parameter` 文档注解）、Lombok（`@Data`/`@RequiredArgsConstructor`/`@Slf4j`）、Redis（通过 RedisUtils 存储临时音频下载 UUID 映射）。