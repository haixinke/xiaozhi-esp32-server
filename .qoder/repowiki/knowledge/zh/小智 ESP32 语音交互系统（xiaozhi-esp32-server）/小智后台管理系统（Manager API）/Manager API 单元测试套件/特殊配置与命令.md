测试依赖本地 Redis（`localhost:6379`，database 0），由 `src/test/resources/application.yml` 中的 Lettuce 连接池配置驱动；部分测试需通过反射设置 `SpringContextUtils.applicationContext` 以支持 i18n 消息解析（见 `PetServiceImplAdoptTest.initMessageSource`）。