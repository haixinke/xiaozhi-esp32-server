# CONTEXT

## 蛋宝宝

### 原型 (Prototype)

宠物的种类身份。当前取值：锦鲤 (KOI)、玉兔 (RABBIT)。`ai_pet.prototype` 字段记录每只宠物所属原型。

### 故事状态 (Story State)

宠物原型维度的共享叙事状态：同一原型的所有宠物共用一条故事线，而非每只宠物各自一条。由故事引擎每小时整点评估切换。

### 大场景 (Big Scene)

故事状态的顶层情境，回答"宠物处于什么生活状态"。示例：在家、旅行、上学、打工。运营可配文本，无封闭枚举。

### 小场景 (Small Scene)

大场景下的具体地点，回答"宠物在哪里"。示例：卧室、北京-故宫、快餐厅。运营可配文本。

### 动作 (Action)

宠物在小场景中正做的事。示例：小憩、看书、故宫红墙前散步。动作文案可自带地点信息，与小场景名不保证互不重复。运营可配文本。

### 当前状态 (Current Status)

注入 LLM 动态上下文的故事快照文案，格式 `{大场景}·{小场景}：{动作}`（如 `在家·卧室：小憩`）。仅当故事状态为 ACTIVE 时注入。

### 同时段图片轮换 (Same-Period Image Rotation)

动作持续时间内、图片时段未变时，故事引擎在当前时段候选背景图中排除正在使用的图后随机换一张，配文随图更新。动作、场景、时长均不变。与"图片时段边界换图"互补：前者同时段内轮换，后者跨时段切换。

### 用户反馈 (User Feedback)

用户对产品使用过程的单向诉求上报：诉求类型 + 文字描述，运营在智控台处理，不回复用户、不可删除。记录于 `ai_feedback`。

### 诉求类型 (Feedback Type)

反馈的分类，取值由字典 `EGG_FEEDBACK_TYPE` 驱动，运营可在智控台字典管理中增删改，小程序拉取失败时回退本地兜底列表。

### 受理编号 (Receipt Number)

反馈提交成功后向用户展示的追溯编号，格式 `FB<yyyyMMdd>-<6位序列>`，由记录 id 与提交日期派生，不入库。

### 年龄区间 (Age Range)

用户自报的年龄段分组，取值由字典 `EGG_AGE_RANGE` 驱动（`AGE_0_14` / `AGE_15_35` / `AGE_36_60` / `AGE_61_PLUS`），记录于 `ai_wechat_user.age_range`，与生日字段相互独立。仅用于年龄分组，不采集完整生日，不参与宠物养成或 LLM 逻辑。进入聊天页前必须已设置，未设置则强制跳转年龄区间页选择；可随时在我的页面修改，不可清除。

### 每日聊天依赖提醒 (Daily Chat Dependency Reminder)

当用户当日发送的聊天消息数（`ai_agent_chat_history` 中 `chat_type=1` 的记录，按用户跨设备合并，Asia/Shanghai 日界）超过 300 条时触发的合规提醒。弹窗文案"长时间 AI 陪伴易产生依赖，请多参与线下户外活动。"；年龄区间为 `AGE_0_14`（≤14 周岁）的用户弹窗后退出聊天页、当日禁聊，成年人可继续使用且当日仅提示一次。计数由后端 `GET /agent/chat-history/daily-user-count` 只读查询，前端每分钟轮询触发；`chatLimited` 表示未成年人当日已超阈值。与订阅制聊天配额（`freeDailyChatLimit`）是两套独立机制，互不覆盖。详见 ADR 0002。

### 一人一宠 (One Pet Per User)

当前产品规则：一个微信用户同一时间只能领养一只蛋宝宝。两条领养入口（邀请码 `adopt`、NFC 触碰 `claim`）共用同一约束与同一错误码 `PET_ALREADY_EXISTS`(10206)；NFC 领取页对该场景展示专属面板「领取失败：您已经领取过蛋宝宝，一个用户暂时只支持领养一个蛋宝宝。」。数据模型 `ai_pet` 保留 1:N 能力（user_id 外键），"暂时"意味着未来可能放开多宠，届时仅需移除该约束检查。

### 手动写卡模式 (Manual Write Mode)

NFC 写卡任务的两种模式之一（另一种为工厂 CSV 模式），创建任务时选定、任务内互斥。面向小批量验证：操作员用手机 NFC App 逐张写入 Scheme，不走 CSV 导出导入，以触碰自验证替代设备回读证据。详见 ADR 0003。

### 触碰自验证 (Touch Self-Verification)

手动写卡模式下的验证方式：用手机真实触碰刚写入的 NFC 标签，微信拉起领取页并命中 preview，即证明 URI 写对、链接有效、页面可打开，资产据此推进为已验证。仅对"已标记已写入且在手动任务中"的资产生效。

### 锁卡 (Tag Lock)

把 NFC 标签设为只读的物理操作，不可逆，防止标签流通后被改写指向钓鱼页。工厂 CSV 模式在写入后立即锁卡并作为导入硬校验；手动写卡模式在验证通过后才锁卡，并作为入库的强制前置。

### 锁后复验 (Post-Lock Re-Verification)

锁卡完成后再触碰一次标签并命中 preview，证明锁卡没有写坏、标签只读后仍可读，资产落 `lock_verified_at`。入库门禁的第二个强制前置（与锁卡并列）。复验的证据价值在"卡可读"本身，不区分触碰者是操作员还是真实用户。preview 的手机号授权门禁不得挡在复验之前——未授权用户的触碰同样触发复验。
