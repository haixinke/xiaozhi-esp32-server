<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xiaozhi.modules.pdc.nfc.dao.PdcNfcAssetDao">

    <select id="selectByClaimHashesForUpdate"
            resultType="xiaozhi.modules.pdc.nfc.entity.PdcNfcAssetEntity">
        SELECT * FROM pdc_nfc_asset
        WHERE claim_ref_hash IN
        <foreach collection="hashes" item="hash" open="(" separator="," close=")">#{hash}</foreach>
        ORDER BY id
        FOR UPDATE
    </select>

    <select id="selectByIdsForUpdate"
            resultType="xiaozhi.modules.pdc.nfc.entity.PdcNfcAssetEntity">
        SELECT * FROM pdc_nfc_asset
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
        ORDER BY id
        FOR UPDATE
    </select>

</mapper>
