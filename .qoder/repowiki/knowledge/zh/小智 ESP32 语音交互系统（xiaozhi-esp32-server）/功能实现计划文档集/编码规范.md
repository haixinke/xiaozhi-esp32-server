- 每个实现计划采用统一结构：Objective → Context Brief → Step N（含 Depends on / Files to create/modify / Tasks / Exit criteria）→ Dependency Graph → Rollback Strategy → File Inventory。
- 后端模块设计严格遵循 Layered Architecture：Entity → DAO(BaseMapper) → DTO/VO → Service(接口+impl) → Controller，并通过 ShiroConfig 注册路由权限、ErrorCode.java 集中定义错误码。
- 数据库变更通过 Liquibase SQL 文件命名 YYYYMMDDHHMM.sql 并按顺序追加到 db.changelog-master.yaml，DDL 使用 utf8mb4 字符集与 InnoDB 引擎。
- 可插拔支付通道通过 WechatPayClient 接口抽象，Mock 与真实实现由 @ConditionalOnProperty 控制，生产默认关闭 mock 强制显式配置。
- 敏感配置（商户私钥、APIv3 密钥）统一通过 AESUtils 以项目 server.secret 加密后存入 sys_params，启动时由 Properties.loadFrom 解密加载。