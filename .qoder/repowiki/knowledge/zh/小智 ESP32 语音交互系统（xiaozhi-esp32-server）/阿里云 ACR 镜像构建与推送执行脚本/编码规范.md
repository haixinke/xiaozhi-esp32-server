- 所有脚本以 `#!/usr/bin/env bash` 开头并使用 `set -euo pipefail` 启用严格模式
- 密码通过环境变量 `ACR_PASSWORD` 或交互式 `read -rsp` 输入，禁止硬编码在脚本中
- 固定用户名 `ACR_USERNAME="nick4941179123"` 作为默认配置，版本号通过可选 `VERSION` 环境变量传入
- 通过 `SCRIPT_DIR` 计算相对路径并使用 `exec` 委派到 `docs/aliyun/sae-deployment/scripts/` 下的具体实现脚本
- 脚本头部包含统一的注释块，说明用途、使用方法及安全说明