- 每个子项目拥有独立的 CLAUDE.md 说明其架构与常用命令，根级 AGENTS.md 统一规定 KISS/DRY/YAGNI、错误处理、输入校验、不可变性等跨语言编码规范
- 所有子项目遵循安全清单：禁止硬编码密钥、用户输入必须校验、SQL 防注入、XSS 防护、错误消息不泄露敏感信息
- 配置文件分层加载：默认 config.yaml → data/.config.yaml（gitignore）→ 远程 manager-api 配置，后层覆盖前层
- 修改代码后需运行 graphify update . 保持 graphify-out/ 图谱与 AST 一致，优先通过图谱导航而非 grep 搜索