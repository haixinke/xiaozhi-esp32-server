启动时通过 `websockets.serve(self._handle_connection, host, port, process_request=self._http_response)` 同时处理 WebSocket 升级与普通 HTTP 请求；连接数上限通过 `server.max_connections` 配置，告警阈值由 `max_connections_warning_ratio` 控制；Token 过期时间默认 30 天，可通过 `server.auth.expire_seconds` 覆盖。