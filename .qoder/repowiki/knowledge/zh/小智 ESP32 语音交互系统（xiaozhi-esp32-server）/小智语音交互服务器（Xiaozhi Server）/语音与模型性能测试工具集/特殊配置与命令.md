每个脚本均可独立运行，例如 `python performance_tester_asr.py`、`python performance_tester_stream_tts.py --text "你好" --count 5`、`python performance_tester_stream_asr.py --count 5`；需要先在 `config/settings.yaml` 的 `ASR/TTS/LLM/VLLM` 节点下填写对应供应商的 API Key/Token 等配置，并在 `config/assets` 目录下放置 >300KB 的 wav 音频文件供 ASR 测试使用。